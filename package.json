{
  "name": "@mnke/circus",
  "version": "0.1.0",
  "module": "index.ts",
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "typecheck": "bun run --filter='*' typecheck",
    "build": "bun run --filter='*' build"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
